import { usePortfolios } from "@/utils/api/queries"

export default function Portfolios() {
    const portfoilos = usePortfolios();
    return (
        <div className="flex flex-row">
        </div>
    )
}